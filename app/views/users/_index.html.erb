<div class="users-index">
	<% users.each do |user| %>
		<div class="users-index-content">
			<%= link_to user_path(user) do %>
				<%= attachment_image_tag user, :profile_image, size:'250x250', fallback: "no_image_icon.jpg" %>
				<h3><%= user.name.truncate(16) %></h3>
			<% end %>
		</div>
	<% end %>
</div>
